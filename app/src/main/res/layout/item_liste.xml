<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/height_edit_text"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".ListAdapter">

    <TextView
        android:id="@+id/memo_intitule"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/padding_edit_text"
        android:layout_marginLeft="@dimen/padding_edit_text"
        android:layout_marginRight="@dimen/padding_edit_text"
        android:layout_marginStart="@dimen/padding_edit_text"
        android:textColor="@android:color/black"
        android:textSize="@dimen/main_taille_intitule_memo"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Mémo"/>

</androidx.constraintlayout.widget.ConstraintLayout>